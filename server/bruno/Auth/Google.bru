meta {
  name: Google
  type: http
  seq: 6
}

post {
  url: {{apiUrl}}/auth/google
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "idToken": "{{googleIdToken}}"
  }
}
